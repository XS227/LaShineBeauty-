# LaShineBeauty-

## Security Notes
- `/storage/` must stay non-public; block direct web access with server rules (nginx/apache).
- `/admin/tracking.html` is a visual placeholder only, not a secure analytics product.
- Add real authentication and authorization before handling real customer data.
